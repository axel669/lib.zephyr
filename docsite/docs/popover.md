# Popover
The Popover component is for displaying content over something without it
being modal (you can interact with things under it still).

## Snippet

### content(show)
The content to show that the popover opens on top of. Is given a function prop
to show the popover.

### popover(hide)
The content of the popover, with a function prop that hides the popover.
