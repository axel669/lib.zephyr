# HexagonSpinner
Zephyr exports the HexagonSpinner component as a wrapper around the Windstorm
component of the same name. This component really doesnt do much other than
wrap the existing one (and make data-ws into ws in the markup).

## Props

{{var:ws}}
